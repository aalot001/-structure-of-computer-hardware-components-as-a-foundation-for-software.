int buf[2] = {1, 2};

int main(){


	printf("Before swaping buf[0] %d and buf[1] = %d\n\n", buf[0], buf[1]);
	swap();

	printf("After swaping buf[0] %d and buf[1] = %d\n\n", buf[0], buf[1]);

	return 0;
}

